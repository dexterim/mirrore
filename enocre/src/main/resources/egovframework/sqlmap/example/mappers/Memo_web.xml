<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.memo.service.impl.MemoWebMapper">

	<insert id="insertMemoService" parameterType="map">
    INSERT INTO MEMO (MEMBER_ID, TITLE, CONTENT) 
    VALUES (#{member_id, jdbcType=VARCHAR}, 
    		#{title}, 
    		#{content})
	</insert>
	
	<update id="updateMemoService" parameterType="map">
		UPDATE MEMO
		SET TITLE = #{title},
		 	CONTENT = #{content}
		WHERE MEMBER_ID = #{member_id}
		AND MEMO_ID = {memo_id};
	</update>
	
	<delete id="deleteMemoService" parameterType="map"> 
		DELETE FROME MEMO 
		WHERE MEMBER_ID = #{id}
		AND TITLE = #{title}
		AND LAST_UPLOAD_DATE = #{last_upload_date};
	</delete>
</mapper>